syntax = "proto3";

package federation;

service FederationService {
  rpc LinkIdentity(LinkIdentityRequest) returns (FederationResponse);
  rpc GetIdentity(GetIdentityRequest) returns (FederationResponse);
}
